<%= image_tag @product.photo if @product.photo.attached? %>

<h2><%= @product.title %></h2>
<p><%= @product.description %></p>
<span>$<%= @product.price %></span>

<%= link_to 'Editar', edit_product_path(@product.id) %>
<%= link_to 'Eliminar producto', product_path(@product), data: { turbo_method: :delete, turbo_confirm: '¿Are you sure?' }%>